<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= errorCode %> - Velink</title>
    <link rel="icon" href="/img/logo/fresh/white_purple_rounded.png" type="image/png">
    <meta name="description" content="<%= errorMessage %> - Velink Error Page">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="/css/base.css" rel="stylesheet">
    <link href="/css/cookie-notice.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            position: relative;
            overflow: hidden;
        }

        .error-container {
            text-align: center;
            z-index: 10;
            max-width: 600px;
            padding: 2rem;
            position: relative;
        }

        .error-code {
            font-size: clamp(4rem, 12vw, 8rem);
            font-weight: 700;
            color: rgba(255, 255, 255, 0.9);
            margin: 0;
            line-height: 1;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        .error-title {
            font-size: clamp(1.5rem, 4vw, 2.5rem);
            font-weight: 600;
            color: white;
            margin: 1rem 0;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
        }

        .error-description {
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.8);
            margin: 1.5rem 0;
            line-height: 1.6;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }

        .error-actions {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
            justify-content: center;
            margin-top: 2rem;
        }

        .btn-error {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.875rem 2rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .btn-error:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            background: rgba(255, 255, 255, 0.2);
            color: white;
            text-decoration: none;
        }

        .btn-error.primary {
            background: rgba(255, 255, 255, 0.95);
            color: var(--primary);
            border-color: transparent;
        }

        .btn-error.primary:hover {
            background: white;
            color: var(--primary);
            transform: translateY(-3px);
        }

        .floating-shapes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
        }

        .shape {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            animation: float 6s ease-in-out infinite;
        }

        .shape:nth-child(1) {
            width: 80px;
            height: 80px;
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }

        .shape:nth-child(2) {
            width: 120px;
            height: 120px;
            top: 60%;
            right: 10%;
            animation-delay: 2s;
        }

        .shape:nth-child(3) {
            width: 60px;
            height: 60px;
            top: 10%;
            right: 20%;
            animation-delay: 4s;
        }

        .shape:nth-child(4) {
            width: 100px;
            height: 100px;
            bottom: 20%;
            left: 20%;
            animation-delay: 1s;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            33% { transform: translateY(-20px) rotate(120deg); }
            66% { transform: translateY(10px) rotate(240deg); }
        }

        .status-link {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }

        .status-link:hover {
            color: white;
            background: rgba(255, 255, 255, 0.2);
            text-decoration: none;
        }

        @media (max-width: 768px) {
            .error-container {
                padding: 1rem;
            }

            .error-actions {
                flex-direction: column;
                align-items: center;
            }

            .btn-error {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }

            .status-link {
                bottom: 1rem;
                right: 1rem;
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="floating-shapes">
        <div class="shape"></div>
        <div class="shape"></div>
        <div class="shape"></div>
        <div class="shape"></div>
    </div>

    <div class="error-container">
        <h1 class="error-code"><%= errorCode %></h1>
        <h2 class="error-title"><%= errorMessage %></h2>
        
        <p class="error-description">
            <% if (errorCode == '404') { %>
                The page you're looking for seems to have vanished into the digital void. It might have been moved, deleted, or perhaps it never existed at all.
            <% } else if (errorCode == '500') { %>
                Our servers are having a temporary hiccup. Our engineering team has been notified and is working on a fix.
            <% } else if (errorCode == '403') { %>
                You don't have permission to access this resource. If you believe this is an error, please contact support.
            <% } else { %>
                Something unexpected happened. Don't worry, it's not your fault - our team is already on it.
            <% } %>
        </p>

        <div class="error-actions">
            <a href="/" class="btn-error primary">
                <i class="bi bi-house"></i>
                Back to Home
            </a>
            <a href="javascript:history.back()" class="btn-error">
                <i class="bi bi-arrow-left"></i>
                Go Back
            </a>
            <% if (errorCode == '404') { %>
                <a href="/register" class="btn-error">
                    <i class="bi bi-person-plus"></i>
                    Create Account
                </a>
            <% } %>
        </div>
    </div>

    <a href="https://status.velyzo.de" target="_blank" rel="noopener noreferrer" class="status-link">
        <i class="bi bi-activity"></i>
        System Status
    </a>

    <!-- Cookie Notice -->
    <div id="cookieNotice" class="cookie-notice">
        <div class="cookie-content">
            <div class="cookie-text">
                <h4>We value your privacy</h4>
                <p>We use cookies to enhance your experience and analyze site usage. Your privacy matters to us.</p>
            </div>
            <div class="cookie-actions">
                <button id="acceptCookies" class="cookie-btn primary">Accept All</button>
                <button id="manageCookies" class="cookie-btn secondary">Manage</button>
            </div>
        </div>
    </div>

    <script src="/js/cookie-manager.js"></script>
</body>
</html>