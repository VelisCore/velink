<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Velink – Coming Soon</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --bg: #ffffff;
      --text: #111111;
      --accent: #4f46e5; /* Indigo */
      --muted: #6b7280;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg);
      color: var(--text);
      line-height: 1.6;
    }

    nav {
      position: sticky;
      top: 0;
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid #e5e7eb;
      z-index: 100;
    }

    nav .logo {
      font-size: 1.2rem;
      font-weight: 700;
      color: var(--accent);
      letter-spacing: -0.5px;
    }

    nav ul {
      display: flex;
      gap: 1.5rem;
      list-style: none;
    }

    nav ul li a {
      text-decoration: none;
      color: var(--text);
      font-weight: 500;
      transition: color 0.3s;
    }

    nav ul li a:hover {
      color: var(--accent);
    }

    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 2rem;
      background: linear-gradient(to bottom, #ffffff 0%, #f9fafb 100%);
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: 700;
      max-width: 700px;
    }

    .hero p {
      font-size: 1.25rem;
      color: var(--muted);
      margin: 1rem 0 2rem;
      max-width: 600px;
    }

    .cta-button {
      position: relative;
      padding: 0.9rem 2rem;
      font-size: 1rem;
      font-weight: 600;
      color: white;
      background-color: var(--accent);
      border: none;
      border-radius: 999px;
      text-decoration: none;
      overflow: hidden;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .cta-button::before {
      content: "";
      position: absolute;
      top: 0;
      left: -100%;
      height: 100%;
      width: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s ease;
    }

    .cta-button:hover::before {
      left: 100%;
    }

    .features {
      padding: 4rem 2rem;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      background-color: #f9fafb;
    }

    .feature {
      background: white;
      border: 1px solid #e5e7eb;
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.02);
      transition: transform 0.2s ease;
    }

    .feature:hover {
      transform: translateY(-4px);
    }

    .feature h3 {
      margin-bottom: 1rem;
      color: var(--accent);
      font-size: 1.2rem;
    }

    .early-access {
      padding: 4rem 2rem;
      text-align: center;
    }

    .early-access a {
      color: var(--accent);
      font-weight: 600;
      text-decoration: none;
      border-bottom: 2px solid transparent;
      transition: border 0.3s ease;
    }

    .early-access a:hover {
      border-color: var(--accent);
    }

    footer {
      padding: 2rem;
      text-align: center;
      font-size: 0.9rem;
      color: var(--muted);
    }

    @media (max-width: 640px) {
      .hero h1 {
        font-size: 2rem;
      }

      .hero p {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>

  <nav>
    <div class="logo">Velink</div>
    <ul>
      <li><a href="#features">Features</a></li>
      <li><a href="#early-access">Early Access</a></li>
      <li><a href="https://discord.gg/NGUy2WBb?event=1368257511681163285" target="_blank">Discord</a></li>
    </ul>
  </nav>

  <section class="hero">
    <h1>Simplify Your Online Presence</h1>
    <p>Velink is a minimalist, customizable alternative to Linktree – launching soon.</p>
    <a href="https://link.velis.me/yourname" class="cta-button">Claim Your Link</a>
  </section>

  <section id="features" class="features">
    <div class="feature">
      <h3>🚀 Minimal & Modern</h3>
      <p>Clean design that puts your links front and center – no clutter, just utility.</p>
    </div>
    <div class="feature">
      <h3>🎨 Custom Profiles</h3>
      <p>Choose your name, colors, and soon – styles! Make your page truly yours.</p>
    </div>
    <div class="feature">
      <h3>📱 Mobile-Ready</h3>
      <p>Optimized for mobile and desktop. Share your links anywhere, beautifully.</p>
    </div>
    <div class="feature">
      <h3>🔗 Easy Link Manager</h3>
      <p>Use a simple dashboard to control what your visitors see – anytime.</p>
    </div>
  </section>

  <section id="early-access" class="early-access">
    <h2>Want Early Access?</h2>
    <p>Join our <a href="https://discord.gg/NGUy2WBb?event=1368257511681163285" target="_blank">Discord</a> to be among the first to try Velink!</p>
  </section>

  <footer>
    &copy; 2025 Velink – Crafted with ♥ by Velis
  </footer>

</body>
</html>
