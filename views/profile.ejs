<!DOCTYPE html>
<html lang="de">
<head>
  <!-- Basis -->
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>@<%= username %> – Velink</title>
  <meta name="description"
        content="Willkommen im Link-Hub von <%= username %> – entdecke Projekte, Socials und mehr.">

  <!-- Open-Graph / Twitter -->
  <meta property="og:title" content="@<%= username %> – Velink">
  <meta property="og:description"
        content="Entdecke Projekte, Socials und mehr von <%= username %>.">
  <meta property="og:image" content="<%= profile.avatar %>">
  <meta property="og:type"  content="website">
  <meta property="og:url" content="<%= url %>">
  <meta name="twitter:card"        content="summary_large_image">
  <meta name="twitter:title"       content="@<%= username %> – Velink">
  <meta name="twitter:description" content="Entdecke Projekte, Socials und mehr von <%= username %>.">
  <meta name="twitter:image"       content="<%= profile.avatar %>">

  <link rel="icon" href="/img/logo/fresh/white_purple_rounded.png" type="image/png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    :root{
      --bg:#0f0f0f;
      --accent:#7c3aed;
      --text:#f3f3f3;
      --card:#1a1a1a;
      --line:#2d2d2d;
      --radius:16px;
    }

    /* Reset / Basics ----------------------------------------------------- */
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    html{scroll-behavior:smooth}
    body{
      font-family:'Inter',system-ui,-apple-system,sans-serif;
      background:var(--bg);
      color:var(--text);
      display:flex;
      flex-direction:column;
      align-items:center;
      padding:2rem 1rem;
    }
    @media (prefers-reduced-motion:reduce){
      *{transition:none!important;animation:none!important}
    }

    /* Scrollbar ---------------------------------------------------------- */
    ::-webkit-scrollbar{width:14px;height:14px;background:var(--bg)}
    ::-webkit-scrollbar-track{
      background:repeating-linear-gradient(45deg,var(--bg),var(--bg) 10px,#e0e0e0 10px,#e0e0e0 20px);
      border:1px solid var(--line)
    }
    ::-webkit-scrollbar-thumb{
      background:var(--accent);
      border:2px solid var(--line);
      box-shadow:inset -1px -1px 0 #222,inset 1px 1px 0 #fff
    }
    ::-webkit-scrollbar-thumb:hover{background:#3b34c1}
    *{scrollbar-width:auto;scrollbar-color:var(--accent) var(--bg)} /* Firefox */

    /* Layout ------------------------------------------------------------- */
    .preview-badge{
      position:fixed;top:1rem;right:1rem;z-index:999;
      background:#f59e0b;color:#000;font-weight:600;
      padding:.4rem .75rem;border-radius:9999px;font-size:.85rem;
      box-shadow:0 4px 12px rgba(0,0,0,.2)
    }
    .profile{max-width:500px;width:100%;text-align:center}
    .avatar{
      width:128px;height:128px;margin-bottom:1rem;
      border:3px solid var(--accent);border-radius:50%;object-fit:cover
    }
    h1{font-size:2rem;font-weight:700}
    .username{color:var(--accent)}
    .badge{
      display:inline-flex;align-items:center;gap:.4rem;
      background:#272727;color:#ccc;font-size:.85rem;
      padding:.25rem .75rem;border-radius:var(--radius);margin:1rem 0
    }
    .badge i{color:#00c2ff}

    .status{margin:1rem 0;color:#aaa;font-style:italic}

    nav.links{
      margin-top:1.5rem;
      display:flex;flex-direction:column;gap:1rem
    }
    .link,.edit-button{
      display:flex;align-items:center;gap:.75rem;
      background:var(--card);color:inherit;text-decoration:none;
      padding:1rem;border-radius:var(--radius);
      box-shadow:0 4px 12px rgba(0,0,0,.15);
      transition:transform .2s,background .2s
    }
    .link:hover,.edit-button:hover{transform:translateY(-3px);background:#222}
    .link i,.edit-button i{font-size:1.3rem;color:var(--accent)}

    .embed{margin-top:2rem;max-width:480px;width:100%;border-radius:12px;overflow:hidden}

    footer{margin-top:3rem;font-size:.8rem;color:#555}
    footer a{color:var(--accent);text-decoration:none}
  </style>
</head>

<body>
  <span class="preview-badge" aria-label="Preview">Preview – Design & Features will change</span>

  <main class="profile" aria-labelledby="profile-name">
    <img class="avatar"
         src="https://avatars.githubusercontent.com/u/158351052?v=4"
         alt="Profilbild von <%= username %>">

    <h1 id="profile-name">@<span class="username"><%= username %></span></h1>

    <span class="badge" aria-label="Verifizierter Creator">
      <i class="bi bi-patch-check-fill" aria-hidden="true"></i>Verified Creator
    </span>

    <% if (profile.bio) { %>
      <p class="status"><%= profile.bio %></p>
    <% } %>

    <nav class="links" aria-label="Hauptlinks">
      <a class="link" href="https://github.com/wfxey" target="_blank" rel="noopener noreferrer">
        <i class="bi bi-github"></i><span>GitHub Projects</span>
      </a>
      <a class="link" href="https://velyzo.de" target="_blank" rel="noopener noreferrer">
        <i class="bi bi-globe"></i><span>Personal Website</span>
      </a>
      <a class="link" href="https://x.com/Velyzo_core" target="_blank" rel="noopener noreferrer">
        <i class="bi bi-twitter-x"></i><span>X / Twitter</span>
      </a>
      <a class="link" href="mailto:wfxey@velyzo.de">
        <i class="bi bi-envelope"></i><span>Contact via Mail</span>
      </a>
      <a class="link" href="https://discord.gg/AjtY2fNCpS?event=1368257511681163285"
         target="_blank" rel="noopener noreferrer">
        <i class="bi bi-discord"></i><span>Join the Discord Event</span>
      </a>
          <% if (isOwnProfile) { %>
      <a class="edit-button" href="/manage">
        <i class="bi bi-pencil-square"></i><span>Edit Profile</span>
      </a>
    <% } %>
    </nav>

    <section class="embed" aria-label="Spotify-Song">
      <iframe src="https://open.spotify.com/embed/track/2AjSclSfX0sn507RxOzpBF?utm_source=generator"
              width="100%" height="80" frameborder="0"
              allow="autoplay; clipboard-write; encrypted-media; picture-in-picture"
              loading="lazy"></iframe>
    </section>

    <footer>
      Powered by <a href="/">Velink</a>
    </footer>
  </main>

  <!-- Ko-fi Widget -->
  <script src="https://storage.ko-fi.com/cdn/scripts/overlay-widget.js" defer></script>
  <script>
    window.addEventListener('DOMContentLoaded',()=> {
      kofiWidgetOverlay.draw('wfxey',{
        type:'floating-chat',
        'floating-chat.donateButton.text':'Support me',
        'floating-chat.donateButton.background-color':'#ffffff',
        'floating-chat.donateButton.text-color':'#323842'
      });
    });
  </script>
</body>
</html>